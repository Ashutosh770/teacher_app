/**
 * Sanity test — verifies the Jest + jest-expo test runner is wired up correctly.
 * Replaced/expanded by real unit and property tests in later tasks.
 */
describe("test runner setup", () => {
  it("runs a trivial passing test", () => {
    expect(true).toBe(true);
  });
});
